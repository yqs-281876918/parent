<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.mixed.exam.student.dao.ExamMapper">
    <select id="findExamByCno" resultType="org.mixed.exam.bank.api.pojo.po.Exam">
        select * from exam
        where classID = #{classID}
    </select>
</mapper>